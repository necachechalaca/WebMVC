@model webmvc.Models.ProdcutModel
@{
    var product = ViewBag.product as ProdcutModel;
}

<p> @ViewData["Title"]</p>
<div class="card bg-primary text-white"> 
    <h1 class="card-header">@product.productName</h1>
    <div class="card-body"> 
        <p class="card-text"> 
            Gia = @product.Price
        </p>
    </div>

</div>